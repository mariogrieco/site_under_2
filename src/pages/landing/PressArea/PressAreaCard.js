import React, { memo } from 'react'
import styles from './PressAreaCard.scss'

const PressAreaCard = () => {
  return (
    <div className={styles.PressAreaCard}>
      
    </div>
  )
}

export default memo(PressAreaCard)
