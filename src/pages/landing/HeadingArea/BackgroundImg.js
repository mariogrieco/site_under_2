import React, { memo } from 'react'

import styles from './BackgroundImg.scss';

const BackgroundImg = () => {
  return (
    <div className={styles.BackgroundImg}>
      &nbsp;
    </div>
  )
}

export default memo(BackgroundImg)
